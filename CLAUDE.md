# CLAUDE.md

The operating manual for this repo lives in **[AGENTS.md](AGENTS.md)** — read it
before working here. It holds the toolchain rules, the framework/content
firewall, and the four runbooks (get started, migrate, embed, update).

`AGENTS.md` is the agent-agnostic single source of truth; this file only points
Claude Code at it so nothing is missed.

**Commit authorship:** never add a `Co-Authored-By: Claude` (or any agent) trailer,
and never record an AI in the author/committer fields — the commit rule in
[AGENTS.md](AGENTS.md) applies to every agent working here, including Claude Code.
